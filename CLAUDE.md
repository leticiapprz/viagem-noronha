# CLAUDE.md

Guia para o Claude Code ao trabalhar neste repositório.

## Antes de mexer no código

Leia [`PROJECT_MAP.md`](PROJECT_MAP.md) — mapeia o projeto (o site inteiro é um `index.html` só) seção por seção: o que cada bloco de código faz, onde editar cada aba/dado, como funciona a sincronização com Firebase. Depois de qualquer mudança (aba nova, forma de sincronizar dados, estrutura de algum dos arrays de dados, arquivo novo/removido), atualize o `PROJECT_MAP.md` antes de considerar a tarefa concluída.

## Projeto

Site estático de planejamento de viagem (Nordeste/Fernando de Noronha, agosto/2026) — sem backend, sem build. Detalhes completos em `PROJECT_MAP.md`.
